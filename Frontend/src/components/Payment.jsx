import React, { useEffect } from "react";

const Payment = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, []);
  return (
    <div className="mt-44">
      <h2>Payment</h2>
    </div>
  );
};

export default Payment;
